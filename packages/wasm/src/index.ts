export type {
  PhysicsBackendBridge,
  WasmBridge,
  WasmAllocator,
  WasmHandle,
  WasmMemoryView,
  WasmModuleInstance,
  WasmModuleLoader,
} from './wasm';

// Placeholder for wasm package
export const placeholder = 'wasm package initialized';
